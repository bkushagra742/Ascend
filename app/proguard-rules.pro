# Add project specific ProGuard rules here.
# Room, Hilt, and kotlinx.serialization ship their own consumer rules via AAR metadata,
# so this file stays mostly empty unless a specific model class needs keep rules for
# reflection-based (de)serialization edge cases discovered during testing.
